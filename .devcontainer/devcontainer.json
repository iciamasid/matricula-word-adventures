
{
    "name": "Capacitor Android Build Environment",
    "image": "mcr.microsoft.com/devcontainers/universal:latest",
    
    "features": {
        "ghcr.io/devcontainers/features/node:1": {
            "version": "20"
        },
        "ghcr.io/devcontainers/features/java:1": {
            "version": "17",
            "jdkDistro": "ms"
        }
    },
    
    "postCreateCommand": "bash -c 'set -e && echo \"🚀 Starting comprehensive setup...\" && export JAVA_HOME=/usr/lib/jvm/msopenjdk-17 && export ANDROID_SDK_ROOT=/usr/local/lib/android/sdk && export ANDROID_HOME=/usr/local/lib/android/sdk && export PATH=$PATH:$ANDROID_SDK_ROOT/cmdline-tools/latest/bin:$ANDROID_SDK_ROOT/platform-tools:$JAVA_HOME/bin && echo \"☕ Java version:\" && java -version && echo \"📦 Installing npm dependencies...\" && npm install --legacy-peer-deps && echo \"🔨 Building the project...\" && npm run build && echo \"⬇️ Downloading Android command line tools...\" && wget -q https://dl.google.com/android/repository/commandlinetools-linux-9477386_latest.zip && unzip -q commandlinetools-linux-9477386_latest.zip && sudo mkdir -p $ANDROID_SDK_ROOT/cmdline-tools && sudo mv cmdline-tools $ANDROID_SDK_ROOT/cmdline-tools/latest && sudo chown -R codespace:codespace $ANDROID_SDK_ROOT && rm commandlinetools-linux-9477386_latest.zip && echo \"📋 Accepting Android licenses...\" && yes | $ANDROID_SDK_ROOT/cmdline-tools/latest/bin/sdkmanager --licenses && echo \"⬇️ Installing Android SDK components...\" && $ANDROID_SDK_ROOT/cmdline-tools/latest/bin/sdkmanager --install \"platforms;android-34\" \"build-tools;34.0.0\" \"platform-tools\" && echo \"📱 Cleaning and adding Capacitor Android platform...\" && npx cap remove android || true && rm -rf android && npx cap add android && echo \"🔄 Syncing Capacitor...\" && npx cap sync android && echo \"⚙️ Configuring Android project...\" && cd android && chmod +x gradlew && echo \"sdk.dir=$ANDROID_SDK_ROOT\" > local.properties && cat > gradle.properties << EOF && # Android SDK and build settings\nsdk.dir=$ANDROID_SDK_ROOT\norg.gradle.java.home=$JAVA_HOME\norg.gradle.jvmargs=-Xmx2048m -XX:+HeapDumpOnOutOfMemoryError -Dfile.encoding=UTF-8\n\n# Android build settings\nandroid.useAndroidX=true\nandroid.enableJetifier=true\n\n# Gradle daemon settings\norg.gradle.daemon=true\norg.gradle.parallel=true\norg.gradle.configureondemand=true\nEOF\ncd .. && echo \"🌍 Setting up environment variables...\" && cat >> ~/.bashrc << EOF && \n# Android Development Environment\nexport ANDROID_SDK_ROOT=$ANDROID_SDK_ROOT\nexport ANDROID_HOME=$ANDROID_SDK_ROOT\nexport JAVA_HOME=$JAVA_HOME\nexport PATH=\\$PATH:$ANDROID_SDK_ROOT/cmdline-tools/latest/bin:$ANDROID_SDK_ROOT/platform-tools:\\$JAVA_HOME/bin\nEOF\necho \"✅ Android build environment setup completed!\"'",
    
    "customizations": {
        "vscode": {
            "extensions": [
                "esbenp.prettier-vscode",
                "dbaeumer.vscode-eslint",
                "ms-vscode.vscode-typescript-next",
                "formulahendry.auto-rename-tag"
            ]
        }
    }
}
